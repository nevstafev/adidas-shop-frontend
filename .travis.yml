language: node_js
node_js:
  - "node"
install:
  - npm install
before_script:
  - npm install -g now
script:
  - npm run lint && npm run build && now -p -t $NOW_TOKEN build && now alias -t $NOW_TOKEN
